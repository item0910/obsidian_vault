# 其他设备首次同步说明

本仓库已将 `.obsidian/community-plugins.json` 从 Git 跟踪中移除，并加入忽略规则。这个文件记录当前设备启用的第三方插件，以后由各设备独立管理。插件程序和其他插件配置仍按仓库现有规则同步。

## 已有设备的首次迁移步骤

适用于另一台电脑或手机首次拉取包含上述变更的提交。请按顺序操作：

1. 暂停自动同步，在该设备的 Obsidian 中禁用旧插件 **Paste image Png to Jpeg**，插件 ID 为 `obsidian-paste-png-to-jpeg`。电脑端保留 **Image Converter** 启用；手机按需要选择插件，并保留 Git Sync 启用。
2. 将该设备自己的 `.obsidian/community-plugins.json` 复制到仓库外备份。应在禁用旧插件后备份，不要使用其他设备的启用列表替代。
3. 拉取仓库更新。电脑端建议先正常退出 Obsidian，再通过 Git 拉取；手机如果通过 Obsidian 内的 Git Sync 拉取，保持旧图片插件禁用即可执行同步。先确认拉取成功；有冲突或报错时先处理，不继续下一步。
4. 拉取结束后，确认 Obsidian 已退出，将第 2 步的备份复制回仓库内的 `.obsidian/community-plugins.json`。首次接收取消跟踪的提交时，Git 可能删除原先受跟踪的这个文件，因此需要恢复。
5. 重新打开 Obsidian，确认旧插件仍然禁用，所需插件正常启用。手机尤其要确认 Git Sync 仍然可用。

## 迁移完成以后

- 正常 Git 拉取不再修改这个启用列表，无需每次同步都备份和恢复。
- 各设备需要分别禁用旧图片插件；一台设备上的禁用操作不会再通过 Git 传到另一台。
- 如果手机同步工具提供“清理未跟踪文件”或“清理被忽略文件”等选项，避免启用会删除此本地配置的清理方式。
- 全新克隆的仓库没有旧的启用列表可备份，直接在该设备上按需启用插件，保持旧图片插件禁用。
- 停用旧插件可以避免它继续误处理同步图片，但不会自动修复已经失效的历史图片链接。

电脑端使用 Image Converter 导入图片后，等处理完成并保存笔记，再将图片和对应笔记一起提交、推送。
