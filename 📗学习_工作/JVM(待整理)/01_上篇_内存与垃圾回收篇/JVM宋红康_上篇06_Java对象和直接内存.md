---
date: 2024-09-07
mtime: 2024-10-15
---

# 六. Java 对象和直接内存

**JVM 虚拟机**; **Java**

**面试题 JVM**

### 问: 对象在 JVM 中是怎么储存的

- 从存储过程来说:

	1. 判断类是否已经加载 (如果未加载, 则加载 - 链接 - 初始化)
	2. 给对象分配内存空间 (如果内存规整 (标记压缩算法形成的内存空间), 则采用指针碰撞, 若内存不规整, 则根据虚拟机维护的空间地址分配)
	3. 处理并发问题 (TLAB)
	4. 初始化分配到的空间 (为对象属性 (fields) 设置默认值, 使其在未赋值情况下可以使用)
	5. 设置对象的对象头
	6. 执行 init 方法进行初始化

- 从对象结构和堆内存分配来说:

	对象分为三个结构:

	1. 对象头: 包含运行时元数据 (哈希值, GC 分代年龄, 锁状态标志, 线程持有的锁, 偏向线程 ID, 偏向时间戳) 和元数据指针 (类型指针, 如果对象类型是数组, 还需要记录数组的长度)
	2. 实例数据: 实例数据的说明,他是对象真正存储的有效信息, 包括程序代码中定义的各种类型的字段 (包括父类继承下来的本身拥有的字段)
		a. 规则 1: 相同宽度的字段总是被分配在一起
		b. 规则 2: 父类中定义的变量会出现在子类之前
		c: 规则 3: 如果有 CompactFields 参数为 true(默认为 true), 子类的窄变量可能插入到父类的变量空间
	3. 对齐填充 (可能存在, 用来使对象长度满足 8 的倍数, (另外, padding 这个概念在 LongAddr 类中通过@sun.misc.Contended 这个注解发挥了妙用))

### 问: 对象头信息里面有哪些东西

- 对象头,包含两部分

	1. 运行时元数据 (Mark Word): 哈希值, GC 分代年龄, 锁状态标志, 线程持有的锁, 偏向线程 ID, 偏向时间戳
	2. 类型指针 (元数据指针 Klass Word): 指向类元数据 InstanceKlass, 确定该对象所属的类型, 如果是数组类型, 还需要记录数组的长度

    对象在内存中除对象头外的剩余内容

	3. 实例数据
	4. padding

## 1. 对象的实例化

### 创建对象的方式

![[附件/image/上篇06_Java对象和直接内存-5.jpg]]
创建对象的方式一共有六种:
1. new :
	1. String s = new String ();
	2. public static TestStatic getObject(){
	           return new Object();
	    }
	3. Buider/ Factory
2. Class 的 newInstance(): 反射的方式只能调用空参的构造器, 权限必须是 public
3. Constructor 的 newInstance: 可以调用类定义的构造器, 对参数权限没有要求 (字节码层面对权限不做要求, 权限只对编译有效)
4. 使用 clone 方法 可以调用空参, 当前类需要实现 Cloneable 接口, 实现 clone 方法
5. 使用反序列化: 从外部: 文件, 网络中获取一个对象的二进制流
6. 第三方库 Objenesis: 使用相关的字节码技术 (https://zhuanlan.zhihu.com/p/27220411)

### 创建对象的步骤 (6 步)

整体:
1. 判断对象对应的类是否加载, 链接, 初始化
2. 为对象分配内存
3. 处理并发安全问题
4. 初始化分配到的空间
5. 设置对象的对象头
6. 执行 init 方法进行初始化

#### 1. 判断对象是否加载, 链接 初始化

虚拟机遇到一条 new 指令，首先去检查这个指令的参数能否在 Metaspace 的常量池中定位到一个类的符号引用，并且检查这个符号引用代表的类是否已经被加载、解析和初始化。（即判断类元信息是否存在）。如果没有，那么在双素委派模式下，使用当前类加载器以 ClassLoader+ 包名 + 类名为 Key 进行查找对应的.class 文件。如果没有找到文件，则抛出 ClassNotFoundException 异常，如果找到，则进行类加载，并生成对应的 Class 类对象

分两种情况:
1. 已经加载的类, 则直接使用, 继续创建对象过程
2. 未加载的类, 通过双亲委派机制 ([[JVM宋红康_上篇02_类加载子系统#4 双亲委派机制]]), 寻找该类对应的 class 文件, 并用对应的 ClassLoader([[JVM宋红康_上篇02_类加载子系统#类加载器的分类]]) 完成类的加载, 如果没有找到, 则报 ClassNotFound 异常

#### 2 为对象分配内存

首先计算对象占用空间大小,接着在堆中划分一块内存给新对象。
如果实例成员变量是引用变量,仅分配引用变量空间即可,即 4 个字节大小。

计算对象占用空间大小并分配内存
如果实例的成员变量是引用变量, 则仅分配引用变量的空间即可, 占 4 个字节大小

内存规整与否取决于垃圾回收器采用的垃圾回收算法, 一般情况下标记清除算法 ([[JVM宋红康_上篇09_垃圾回收器相关#标记清除算法]]) 不规整, 标记 - 压缩算法 ([[JVM宋红康_上篇09_垃圾回收器相关#标记-压缩算法]]) 规整

- 内存规整的情况: 采用指针碰撞

如果内存是规整的， 那么虚拟机将采用的是指针碰撞法 (Bump The Pointer) 来为对象分配内存。
意思是所有用过的内存在一边，空闲的内存在另外一边，中间放着一个指针作为分界点的指示器，分配内存就仅仅是把指针向空闲那边挪动一段与对象大小相等的距离罢了。如果垃圾收集器选择的是 Serial、Par New 这种基于压缩算法的， 虚拟机采用这种分配方式。一般使用带有 compact(整理) 过程的收集器时， 使用指针碰撞。

- 内存不规整的情况: 维护一个空闲列表

如果内存不是规整的，已使用的内存和未使用的内存相互交错，那么虚拟机将采用的是空闲列表法来为对象分配内存。
意思是虚拟机维护了一个列表，记录上哪些内存块是可用的，再分配的时候从列表中找到一块足够大的空间划分给对象实例，并更新列表上的内容。这种分配方式成为“空闲列表（Free List）"。

选择哪种分配方式 Java 堆是否规整决定，而 Java 堆是否规整又由所采用的垃圾收集器是否带有压缩整理功能决定。

#### 3 处理并发安全问题

##### 采用 cas 失败重试, 区域加锁保证更新的原子性 (待补充)

##### 每个线程预先分配一块 TLAB([[JVM宋红康_上篇04_堆#什么是TLAB]])

通过 -XX: +/-UseTLAB 参数来使用

#### 4 初始化分配到空间

给对象的属性赋值的操作：
①属性的默认初始化 -②显式初始化 -③代码块中初始化 -④构造器中初始化

指的是对象的默认初始化

```java
class Customer{
	int id; 
	String name;
	Account acct;
}

public static void main(String[] args){
	Customer c = new Customer();
}
```

比如对象中的这三个值,
则会依次赋值为 0, null , null; 目的, 为了在不赋值的情况下可以直接使用

#### 5 设置对象对象头

将对象的所属类 (即类的元数据信息)、对象的 HashCode 和对象的 GC 信息、锁信息等数据存储在对象的对象头中。这个过程的具体设置方式取决于 JVM 实现。
对象头
([[#2 对象的内存布局]])

#### 6 执行 init 方法进行初始化

在 Java 程序的视角看来，初始化才正式开始。初始化成员变量，执行实例化代码块，调用类的构造方法，并把堆内对象的首地址赋值给引用变量。
因此一般来说（由字节码中是否跟随有 invokespecial 指令所决定）,new 指令之后会接着就是执行方法，把对象按照程序员的意愿进行初始化，这样一个真正可用的对象才算完全创建出来。

给对象的属性赋值的操作：
①属性的默认初始化 -②显式初始化 -③代码块中初始化 -④构造器中初始化

```java
public class Customer{
	int id = 1001;
	String name;
	Account acct;
	{
		name = "匿名客户";
	}
	public Customer(){
		acct = new Account();
	}
}
```

## 2. 对象的内存布局

![[附件/image/上篇06_Java对象和直接内存-4.jpg]]
三部分:
1. 对象头,包含两部分
	1. 运行时元数据: 哈希值, GC 分代年龄, 锁状态标志, 线程持有的锁, 偏向线程 ID, 偏向时间戳
	2. 类型指针: 只想类元数据 InstanceKlass, 确定该对象所属的类型
对象头的说明: 如果对象是数组, 还需要记录数组的长度
2. 实例数据:
	1. 规则 1: 相同宽度的字段总是被分配在一起
	2. 规则 2: 父类中定义的变量会出现在子类之前
	3. 规则 3: 如果有 CompactFields 参数为 true(默认为 true), 子类的窄变量可能插入到父类的变量空间
实例数据的说明, 他是对象真正存储的有效信息, 包括程序代码中定义的各种类型的字段 (包括父类继承下来的本身拥有的字段)
3. 对其填充: 不是必须, 也没有特殊的含义, 仅仅起到占位符的作用 (类似快递的泡沫)

### 小结和图示

## 3. 对象的访问定位

### 图示

![[附件/image/上篇06_Java对象和直接内存-6.jpg]]

### 访问方式

#### 句柄访问

![[附件/image/上篇06_Java对象和直接内存-7.jpg]]
- 缺点:
	- 需要专门开辟句柄空间,效率较低
- 优点:
	- 对象发生移动时, 只需要改变句柄池内对象的地址 (垃圾回收时), 无序改变 java 栈中的地址

- 直接指针 (HotSpot 采用)

![[附件/image/上篇06_Java对象和直接内存-8.jpg]]

## 4. 拓展: 直接内存

### 概述

- 不是虚拟机运行时数据区的一部分，也不是《Java 虚拟机规范》中定义的内存区域。
- 直接内存是在 Java 堆外的、直接向系统申请的内存区间。
- 来源于 NIO，通过存在堆中的 DirectByteBuffer 操作 Native 内存
- 通常，访问直接内存的速度会优于 Java 堆。即读写性能高。
    - 因此出于性能考虑，读写频繁的场合可能会考虑使用直接内存。
    - Java 的 NIO 库允许 Java 程序使用直接内存，用于数据缓冲区

NIO : new IO 或者 non-blocked IO

### 图解

NIO 性能要优于普通 IO

![[附件/image/上篇06_Java对象和直接内存-9.jpg]]
![[附件/image/上篇06_Java对象和直接内存-10.jpg]]

### 直接内存的问题和参数

- 也可能导致 OutofMemoryError 异常·由于直接内存在 Java 堆外，因此它的大小不会直接受限于 -Xmx 指定的最大堆大小，但是系统内存是有限的，Java 堆和直接内存的总和依然受限于操作系统能给出的最大内存。
- 缺点
    - 分配回收成本较高
    - 不受 JVM 内存回收管理
- 直接内存大小可以通过 MaxDirectMemorySize 设置
- 如果不指定，默认与堆的最大值 -Xmx 参数值一致

```java
D：\developer_tools\jdk\jdk 1.8.0_131\bin\java.exe..
181
Exception in thread“main"java.lang .OutOfMemoryError：Direct buffer r memory 
	at java.nio.Bits.reserve Memory (Bits.java： 693)
	at java.nio.Direct ByteBuffer.c in it>(Direct ByteBuffer .java： 123
	at java.nio.ByteBuffer.allocate Direct (ByteBuffer.java： 311)
	at com.at gui gu.java.Buffer Test 2.main(Buffer Test 2.java：20)

Process finished with exitcode 1
```

java 内存空间 = heap + 本地内存
![[附件/image/上篇06_Java对象和直接内存-11.jpg]]

————————————————
