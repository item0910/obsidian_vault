---
cards-deck: 算法专项练习:哈希表
mtime: 2024-10-26
---

# 算法专项练习:哈希表

[1. 两数之和](https://leetcode.cn/problems/two-sum)  #card
**数组排序和查找** **哈希表**
1. 准备一个哈希表, 一边找的同时一边存

[128. 最长连续序列](https://leetcode.cn/problems/longest-consecutive-sequence)  #card
**哈希表** **数组排序和查找**
1. 把所有数字放在一个 set 里
2. 开始计算:
	1. 对 set 中的每一个数字:
		1. 如果 num - 1 不在 set 中, 开始: count = 1, curNum = num
		2. 当 set 有 num + 1 时, curNum++, count++
		3. 完后更新 res

[437. 路径总和 III](https://leetcode.cn/problems/path-sum-iii) #card
**二叉树** **回溯** **哈希表**
1. 准备一个前缀和 Map, 先放 put((long)0, 1); 前缀和问题都要放的
2. 前缀和函数: preSumCount(root, preSumMap, targetSum, 0);
3. 先加 curSum
	1. 找到前缀和为 curSum - targetSum 的, 加到 res 上
	2. 前缀和 Map 里添加一条 curSum 的记录
	3. 把左右的递归结果都加到 res 上
	4. 前缀和 Map 里去除一条 curSum 的记录 (回溯)

[560. 和为 K 的子数组](https://leetcode.cn/problems/subarray-sum-equals-k) #card
**哈希表**
1. 前缀和问题,
2. 到一个位置, 先计算前缀和 preSum, 然后看有没有前缀和 preSum - k 的, 如果有, count 累加之
3. 把当前前缀和放到 map 中

[207. 课程表](https://leetcode.cn/problems/course-schedule) #card
**图** **哈希表**
1. 准备 2 个结构:
	1. 课程 Map<编号, List<编号>>: 课程号和完成后可以完成的课程
	2. inDegree[], 每个课程的入度 (需要提前完成的课的数量)
2. 把入度为 0 课程号的放入 Queue 中
3. 处理 queue:
	1. visit = 0;
	2. q 不为空时
		1. 弹出 int from = q.poll()
		2. visit++; 如果 map 中有这个, 那么就把这个课程找出来, 对应所有可以完成的课程的入度 --; 如果这个入度 == 0, 那么将他也放入 queue 中

[139. 单词拆分](https://leetcode.cn/problems/word-break) #card
**暴力递归** **动态规划** **字符串** **哈希表**
1. 暴力递归
	1. 把 Dict 变成 set
	2. process(0, s, set)
		1. 如果 index == s.length(): true
		2. i 从 index + 1 开始,
			1. 如果 set 有 substring(index, i), flag = process(i, s, set)
				1. 如果 flag = true, 那么就 return true
				2. 否则 continue
2. 改动态规划
	1. dp[n +1], dp[n] = true;
	2. i = n - 1 时, i-- : (把 index 看作 j, i 从后往前进行推算)
		1. j = i + 1, j <= s.length(); j++: 如果 dp[j] && wordDict.contains(s.substring(i, j)) : dp[i] = true;
	3. return dp[0];

[399. 除法求值](https://leetcode.cn/problems/evaluate-division) #card
**图** **动态规划** **哈希表**
1. 准备几个结构:
	1. valueMap<String, Integer>, 将每一个出现的字符对应一个 value++ 存入 map 中
	2. `graph[][]` 用来记录已知的值, 先全部填好 -1
		1. 对每一个等式, va = 等式 [0], vb = 等式 [1], 存入 graph 中
	3. 从 k = 0 到 n, 如果 graph 中有 i ->k 的值并且有 k -> j 的值, 那么这个值就有两个值相乘获得
2. 查询 :
	1. 对每一个 query, 如果被除数和除数都存在, 那么找出这个坐标对应的值即可

[17. 电话号码的字母组合](https://leetcode.cn/problems/letter-combinations-of-a-phone-number) #card
**哈希表** **回溯**
1. 把数字和对应的字符串存到 map 中
2. 当 index = digits.length() 时, 加入
3. 回溯这个过程, 记得删除

[49. 字母异位词分组](https://leetcode.cn/problems/group-anagrams) #card
**字符串** **哈希表**
1. char[] str sort 一下, 然后以它为 key 放进 map 中, Map<String, List>
2. 把这个 map 转换成 `ArrayList<List<String>>`

[448. 找到所有数组中消失的数字](https://leetcode.cn/problems/find-all-numbers-disappeared-in-an-array) #card
**数组排序和查找** **哈希表**
1. 方法 1
	1. 把数字作为下标, int index= (n - 1) % n; 然后在 nums[index] + n
	2. 看下哪些下标没有加 n, +1 就是消失的数字
2. 计算词频
