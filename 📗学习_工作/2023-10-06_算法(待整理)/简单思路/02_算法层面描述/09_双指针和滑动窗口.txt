---<br>cards-deck: 算法专项练习:双指针和滑动窗口<br>date: 2024-09-07<br>mtime: 2024-09-25<br>---<br># 算法专项练习: 双指针和滑动窗口<br>[11. 盛最多水的容器](https://leetcode.cn/problems/container-with-most-water) #card<br> **贪心** **双指针和滑动窗口**<br>1. 先用左右比较矮的那个作为高度乘以距离计算结果<br>2. 如果左边比较矮, 左边往中间移, 同理右边也是.<br>[15. 三数之和](https://leetcode.cn/problems/3sum) #card<br>**数组排序和查找** **双指针和滑动窗口**<br>1. 如不够三数, 返回<br>2. 整理数组<br>3. 从左往右,<br>   1. 如果 nums[i] &amp;gt; 0, return 结果; i&amp;gt;=1 时, 如果 nums[i] == num[i - 1], continue<br>   2. L = i + 1, R = n - 1; L &amp;lt; R 时, sum 算出来, 如果<br>      1. sum == 0, 加到 res 里, Arrays.asList(三个数)<br>      2. 然后移动 L, 移动 R,条件 L+1 &amp;lt; R, R - 1 &amp;gt; L<br>      3. 再默认移动一下 L, R(两个一定是同步移动, 否则不可能成立)<br>      4. 然后再根据 sum 的大小情况, 移动 L 和 R<br><ul><li>总结, 一共要移动三次 L, R</li></ul>[42. 接雨水](https://leetcode.cn/problems/trapping-rain-water) #card<br> **单调栈** **双指针和滑动窗口**<br>1. 单调栈<br>1. 核心, 找到两边比自己大的<br>2. 栈不为空 且 如果要进去的那个柱子大于栈顶, 那么弹出<br>1. 记录弹出为 top, 如果此时栈空, 说明没有下面没有比他高的, 无法盛水, break<br>2. left = stack.peek(), 记录左边的柱子;<br>3. 计算能盛宽度, 能乘的单位是多少, 高度 : Math.min(左, 右) - height[i], 宽度: i - left - 1; 累加之<br>3. stack.push(i);<br>2. 左右最高数组法:<br>1. 每个位置能接的水取决于左右比自己高的数组最低的位置<br>2. 准备两个数组 left[], right[], 分别表示比自己高的左右数组<br>1. 两个数组都可以用 left[i -1], 和 right[i + 1], 推导得出<br>2. 最后统一计算即可<br>3. 双指针 O(1) 法, leftMax, height[left], rightMax, height[right] 四个变量唱歌<br>[5. 最长回文子串](https://leetcode.cn/problems/longest-palindromic-substring) #card<br>**字符串** **双指针和滑动窗口**<br>1. 函数: str = getManacherStr<br>2. 参数: pArr(manacher 串长度), C = -1, R= -1, resIndex = 0, maxLength = -1;<br>3. pArr 的基础值根据 R &amp;gt; i ? 判断, 并更新 resIndex, maxLength, C, R<br>4. 用 sb 根据 resIndex 和 maxLength 求出来, 返回.<br>[647. 回文子串](https://leetcode.cn/problems/palindromic-substrings) #card<br>**字符串** **双指针和滑动窗口**<br>1. 其余跟 manacher 最长回文子串过程一样, ans += pArr[i] / 2;<br>[76. 最小覆盖子串](https://leetcode.cn/problems/minimum-window-substring) #card<br>**字符串** **双指针和滑动窗口**<br>1. 先建词频 map, sMap 和 tMap, tMap 先按照 t 登记<br>2. l = 0, r = 0;cnt = 0<br>1. sMap.put R 位置上的字母,<br>2. 如果 tMap 有这个字母并且数量比 sMap 的多, 那么 cnt++<br>3. 如果 l &amp;lt; r 并且 tMap 中没有 l 位置上的字母或者 tMap 上的多于 sMap 里的, sMap 可以 - 1, L++<br>4. 如果 cnt == t.length 并且 r - l + 1 &amp;lt; len; 更新结果<br>[3. 无重复字符的最长子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters) #card<br>**双指针和滑动窗口** **字符串**<br>1. 准备一个 memory, 存放已经走过的路程<br>2. 关键: 当 L &amp;gt; 0 时, 先缩一步左, 再试试能不能往右<br>   往右时, 先判断能否往右, 再移动;<br>3. R 都移动完了 (不能再移动了), L++;<br>[239. 滑动窗口最大值](https://leetcode.cn/problems/sliding-window-maximum) #card<br>**双指针和滑动窗口** **单调栈**<br>1. 准备 res 和栈, index 用来表示 res 的下标<br>2. 栈来维护最大值, 等于时候可弹可不弹<br>1. 如果栈顶元素为 i - k(比如窗口是 3, i = 3 时, 实际窗口长度已经为 4 了) 时, 弹出栈顶<br>2. i &amp;gt;= k - 1 时, 开始输出值, res[index++] = 栈顶<br>