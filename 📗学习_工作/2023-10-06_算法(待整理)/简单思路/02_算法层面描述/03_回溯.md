---
cards-deck: 算法专项练习:回溯
date: 2024-09-07
mtime: 2024-09-25
---

# 算法专项练习: 回溯

[22. 括号生成](https://leetcode.cn/problems/generate-parentheses) #card
 **括号问题** **回溯**
1. 回溯过程 process(int left, int right, int n, StringBuilder sb)
	1. left, right 代表左右已经使用的个数, n 代表需要使用的个数
	2. 终止条件 sb.length == 2 * n
2. process 后 left + 1, 或者 right + 1, 返回该层要把上次加的删除

[78. 子集](https://leetcode.cn/problems/subsets) #card
 **回溯**
1. res 上来就先 add 这个 path
2. 循环中:
	1. 从 index 开始, path 把这个 nums[i] 填进去
	2. process(i + 1, nums, path);
	3. 再把这个最后一个去掉
- 理解: 第二次再开始循环, 进去的就是 index + 1 位置的为首了, 所以不会有重复.

[437. 路径总和 III](https://leetcode.cn/problems/path-sum-iii) #card
**二叉树** **回溯**
1. 准备一个前缀和 Map, 先放 put((long)0, 1); 前缀和问题都要放的
2. 前缀和函数: preSumCount(root, preSumMap, targetSum, 0);
3. 先加 curSum
	1. 找到前缀和为 curSum - targetSum 的, 加到 res 上
	2. 前缀和 Map 里添加一条 curSum 的记录
	3. 把左右的递归结果都加到 res 上
	4. 前缀和 Map 里去除一条 curSum 的记录 (回溯)

[301. 删除无效的括号](https://leetcode.cn/problems/remove-invalid-parentheses) #card
**括号问题** **回溯**
1. 计算要删除的数量:
	1. 如果是 '(', left++
	2. 否则如果是 ')', 如果 left > 0, left--; 否则, right++. left 和 right 就是要删除的括号数量
2. 回溯过程 retrace(s, 0, left, right)
	1. 如果 left = 0, right = 0, 判断是不是 valid
	2. i 从 start 开始
		1. 如果 i != start, 并且 i - 1 和 i 一样, continue
		2. left + right > len - i, return
		3. left > 0 retrace(s.substring(0, i) + s.substring(i+1), i, left - 1, right) : 注意这里新 s 长度和下标的关系
		4. right 同理
3. 判断 isValid
	1. int cnt = 0
	2. if(左括号), cnt++; else if(右括号), cnt--, if(cnt < 0): false
	3. return cnt == 0;

[322. 零钱兑换](https://leetcode.cn/problems/coin-change) #card
**暴力递归** **动态规划**
1. findWay(coins, 0, amount)
	1. 如果 amount < 0, return Integer.MAX_VALUE; 非法值
	2. 如果 amount = 0, return 0(意思不需要硬币了)
	3. 从 index 开始
		1. p1 = 不取这一种
		2. p2 默认 MAX_VALUE
			1. 如果可以取这种硬币并且取完之后不是非法值: p2 = 取这种之后 + 1;
		3. return min(p1, p2)
2. 改动态规划

[17. 电话号码的字母组合](https://leetcode.cn/problems/letter-combinations-of-a-phone-number) #card
**哈希表** **回溯**
1. 把数字和对应的字符串存到 map 中
2. 当 index = digits.length() 时, 加入
3. 回溯这个过程, 记得删除

[39. 组合总和](https://leetcode.cn/problems/combination-sum) #card
**暴力递归** **回溯**
1. 整理数组
2. 回溯过程: 0, candidates, target, 0
	1. sum = target : res.add(path)
	2. 从 index 开始, 条件 i < length && sum + candidates[i] <= target
		1. sum += can[i]; path.add(can[i])
		2. process(i, candidates, target, sum)
		3. sum -= can[i], path.remove

[46. 全排列](https://leetcode.cn/problems/permutations) #card
**回溯** **暴力递归** **哈希表**
1. backTrace(nums)
	1. i 从 0 开始
		1. if(!set.contains(nums[i])) : path.add(nums[i]), backTrace(nums), path.remove()

[79. 单词搜索](https://leetcode.cn/problems/word-search) #card
**字符串** **动态规划** **回溯**
1. flag = check(board, isPassed, i, j, 0, word); if(flag), return true
2. check(board, isPassed, i, j, step, word);
	1. 如果 board 不对, return false;
	2. 否则如果 step = word.length, 返回 true
	3. 先把 isPassed 设置为 true, 预先设置四个方向数组
	4. 判断能走 : 然后 flag = check(new 参数)
		1. 如果 flag, return true
	5. 回溯: isPassed 设置回去
	6. return false
