---
date: 2024-09-07
mtime: 2024-09-25
---

# 课题: 进入尝试

# 汉诺塔

- 把问题分解成第 N 层和前 N-1 层的问题
- 三个基点: String from, String to, String other
- 问题拆分:
	- 第一步: 把 N-1 层从 from 移到 other
	- 中间, 把 N 从 from 移到 to
	- 第三步: 把 N-1 层从 other 移到 to

# 字符串递归

## 字符串的子序列

## 字符串的全排列

# 用递归逆序一个栈

- 先做一个能把最后栈底元素返回, 其他栈元素顺序不变的递归

```java
public int f(Stack stack){
	int result = stack.pop();
	if(stack.isEmpty()){
		return result
	}
	int last = f(stack);
	stack.push(result);
	return last;
}

public void reverse(Stack stack){
	if(stack.isEmpty()){
		return;
	}
	int i = f(stack);
	//新开一个方法栈, 存i
	reverse(stack);
	//利用栈(方法栈)后进先出的特性
	stack.push(i);
}
```

# 数 IsLands

```java
package class013;

/**
 * @author Item
 */
public class Code03_CountIslands {
    public static int countIslands(int[][] arr) {
        if (arr == null || arr.length == 0) {
            return 0;
        }
        int N = arr.length;
        int M = arr[0].length;
        int res = 0;
        for (int i = 0; i < N; i++) {
            for (int j = 0; j < M; j++) {
                if (arr[i][j] == 1) {
                    res++;
                    infect(arr, i, j, N, M);
                }
            }
        }
        return res;
    }

    public static void infect(int[][] arr, int i, int j, int n, int m) {
        if (i < 0 || i >= n || j < 0 || j >= m || arr[i][j] != 1) {
            return;
        }
        arr[i][j] = 2;
        infect(arr, i + 1, j, n, m);
        infect(arr, i - 1, j, n, m);
        infect(arr, i, j + 1, n, m);
        infect(arr, i, j - 1, n, m);
    }
}

```

- 如何设计一个并行算法?
	- 并查集:
		- isSameSet
		- union
	- 从不同的边界出发:
		- 收集岛信息和边界信息
		- 查看边界相碰的情况, union 两个, 然后岛数 -1;
		- 每个 cpu 收集四个边界信息, 然后合并
![[附件/image/12 常见的递归.jpg]]
![[附件/image/12 常见的递归-1.jpg]]
