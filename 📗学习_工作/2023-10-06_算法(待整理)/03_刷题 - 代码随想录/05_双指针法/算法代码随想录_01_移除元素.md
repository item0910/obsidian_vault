---
date: 2024-09-07
mtime: 2024-09-25
---

# 027 移除元素

```java
//首尾互换法
class Solution {
    public int removeElement(int[] nums, int val) {
        int index = 0;
        int length = nums.length;
        while(index < length){
            if(nums[index] != val){
                index++;
            }else{
                swap(nums, index, --length);
            }
        }
        return length;
    }

    public void swap(int[] arr, int index1, int index2){
        int temp = arr[index1];
        arr[index1] = arr[index2];
        arr[index2] = temp;
    }
}
```

```java
//双指针法
class Solution {
    public int removeElement(int[] nums, int val) {
        int slow = 0;
        for(int fast = 0; fast < nums.length; fast++){
            if(val != nums[fast]){
                nums[slow++] = nums[fast];
            }
        }
        return slow;
    }
}
```

# 26 删除有序数组中的重复项

```java
class Solution {
    public int removeDuplicates(int[] nums) {
        if(nums == null || nums.length == 0){
            return 0;
        }
        if(nums.length == 1){
            return 1;
        }

        int slow = 1;
        int fast = 1;
        while(fast < nums.length){
            if(nums[fast - 1] != nums[fast]){
                nums[slow] = nums[fast];
                slow++;
            }
            fast++;
        }
        return slow;
    }
}
```

# 283.移动零

```java
class Solution {
    public void moveZeroes(int[] nums) {
        if(nums == null || nums.length == 0){
            return;
        }
        if(nums.length == 1 && nums[0] == 0){
            return;
        }
        int zero = 0;
        int notZero = 0;
        while(notZero < nums.length){
            if(nums[notZero] != 0){
                nums[zero] = nums[notZero];
                zero++;
            }
            notZero++;
        }
        int zeroCount = nums.length - zero;
        for(int i = nums.length - 1; zeroCount > 0; zeroCount--, i--){
            nums[i] = 0;
        }
    }
}
```
