---
date: 2024-09-07
mtime: 2024-09-25
---

# 459. 重复的子字符串

```java
//去头去尾, 如果还存在原字符串,那么原字符串一定存在重复.
class Solution {
   public boolean repeatedSubstringPattern(String s) {
        String str = s + s;
        return str.substring(1, str.length() - 1).contains(s);
    }
}
```

```java
//KMP
class Solution {
    public boolean repeatedSubstringPattern(String s) {
        if(s.length() == 1){
            return false;
        }
        String a = s + s;
        a = a.substring(1, a.length() - 1);
        return kmp(a, s);

    }
    public boolean kmp(String a, String b){
        int i1 = 0;
        int i2 = 0;
        char[] arr1 = a.toCharArray();
        char[] arr2 = b.toCharArray();
        int[] next = getNextArray(b);
        while(i1 < a.length() && i2 < b.length()){
            if(arr1[i1] == arr2[i2]){
                i1++;
                i2++;
            }else if(i2 == 0){
                i1++;
            }else{
                i2 = next[i2];
            }
        }
        return i2 == arr2.length;
    }

    public int[] getNextArray(String s){
        char[] arr = s.toCharArray();
        int[] next = new int[s.length()];
        next[0] = -1;
        next[1] = 0;
        int cn = 0;
        int i = 2;
        while(i < s.length()){
            if(arr[i - 1] == arr[cn]){
                next[i++] = ++cn;
            }else if(cn > 0){
                cn = next[cn];
            }else{
                next[i++] = 0;
            }
        }
        return next;
    }
}
```

[代码随想录 (programmercarl.com)](https://www.programmercarl.com/0459.%E9%87%8D%E5%A4%8D%E7%9A%84%E5%AD%90%E5%AD%97%E7%AC%A6%E4%B8%B2.html#%E5%85%B6%E4%BB%96%E8%AF%AD%E8%A8%80%E7%89%88%E6%9C%AC)

```java
//周期整除判断
class Solution {
    public boolean repeatedSubstringPattern(String s) {
        if (s.equals("")) return false;

        int len = s.length();
        // 原串加个空格(哨兵)，使下标从1开始，这样j从0开始，也不用初始化了
        s = " " + s;
        char[] chars = s.toCharArray();
        int[] next = new int[len + 1];

        // 构造 next 数组过程，j从0开始(空格)，i从2开始
        for (int i = 2, j = 0; i <= len; i++) {
            // 匹配不成功，j回到前一位置 next 数组所对应的值
            while (j > 0 && chars[i] != chars[j + 1]) j = next[j];
            // 匹配成功，j往后移
            if (chars[i] == chars[j + 1]) j++;
            // 更新 next 数组的值
            next[i] = j;
        }

        // 最后判断是否是重复的子字符串，这里 next[len] 即代表next数组末尾的值
        if (next[len] > 0 && len % (len - next[len]) == 0) {
            return true;
        }
        return false;
    }
}
```
