---
title: 37_Redis数据倾斜
date: 2022-09-08
categories: Redis
tags: 归档笔记/学习笔记/Redis
author: 蒋德钧
mtime: 2024-10-15
---

## 学习之前: 数据倾斜的种类

1. 数据量倾斜: 数据分布不均衡
2. 数据访问倾斜: 热点数据被访问的额外频繁

## Redis 数据倾斜

### 1. 数据量倾斜的成因

1. 保存了 bigkey: 将数据切片, 避免在一个 key 上保存太多数据

#### slot 分配不均衡:

1. 分配 slot 的时候, 并不知道 slot 会打到的数据情况, 结果恰好把几个大数据都打到同一个人实例上: 解决, 观察, 迁移.

	```sh
	127.0.0.1:6379> cluster slots
	1) 1) (integer) 0
	   2) (integer) 4095
	   3) 1) "192.168.10.3"
		  2) (integer) 6379
	2) 1) (integer) 12288
	   2) (integer) 16383
	   3) 1) "192.168.10.5"
		  2) (integer) 6379
	```

	- CLUSTER SETSLOT：使用不同的选项进行三种设置，分别是设置 Slot 要迁入的目标实例，Slot 要迁出的源实例，以及 Slot 所属的实例。
	- CLUSTER GETKEYSINSLOT：获取某个 Slot 中一定数量的 key。
	- MIGRATE：把一个 key 从源实例实际迁移到目标实例。
	- 第 1 步，我们先在目标实例 5 上执行下面的命令，将 Slot 300 的源实例设置为实例 3，表示要从实例 3 上迁入 Slot 300。

	```sh
	CLUSTER SETSLOT 300 IMPORTING 3
	```

	- 第 2 步，在源实例 3 上，我们把 Slot 300 的目标实例设置为 5，这表示，Slot 300 要迁出到实例 5 上，如下所示：

	```sh
	CLUSTER SETSLOT 300 MIGRATING 5
	```

	- 第 3 步，从 Slot 300 中获取 100 个 key。因为 Slot 中的 key 数量可能很多，所以我们需要在客户端上多次执行下面的这条命令，分批次获得并迁移 key。

	```sh
	CLUSTER GETKEYSINSLOT 300 100
	```

	- 第 4 步，我们把刚才获取的 100 个 key 中的 key1 迁移到目标实例 5 上（IP 为 192.168.10.5），同时把要迁入的数据库设置为 0 号数据库，把迁移的超时时间设置为 timeout。我们重复执行 MIGRATE 命令，把 100 个 key 都迁移完。

	```sh
	MIGRATE 192.168.10.5 6379 key1 0 timeout
	```

	- 最后，我们重复执行第 3 和第 4 步，直到 Slot 中的所有 key 都迁移完成。从 Redis 3.0.6 开始，你也可以使用 KEYS 选项，一次迁移多个 key（key1、2、3），这样可以提升迁移效率。

	```sh
	MIGRATE 192.168.10.5 6379 "" 0 timeout KEYS key1 key2 key3
	```

	- 对于 Codis 来说，我们可以执行下面的命令进行数据迁移。其中，我们把 dashboard 组件的连接地址设置为 ADDR，并且把 Slot 300 迁移到编号为 6 的 codis server group 上。

	```sh
	codis-admin --dashboard=ADDR -slot-action --create --sid=300 --gid=6
	```

#### HashTag

![[附件/image/37_Redis数据倾斜.jpg]]
被 Hashtag 包裹的数据会单独计算 CRC16 的值, 利用这个特性把一些数据针对性的存到同一个实例中, 方便提取到客户端做聚合计算.
- 一个不好的结果就是把大量数据集中到了一个实例上. 为了防止业务不稳定, 最好采用别的方式.

### 2. 数据访问倾斜的成因

- 根本原因就是存在热点数据
	- 解决方法: 热点数据多副本, 在 key 前加随机前缀, 让他映射到不同的 slot 中
	- 这种方法只能应对热点数据只读的情况.

### 总结

![[附件/image/37_Redis数据倾斜-1.jpg]]

## 评论区收获和思考

- 在有数据访问倾斜时，如果热点数据突然过期了，而 Redis 中的数据是缓存，数据的最终值保存在后端数据库，此时会发生什么问题？
	- 缓存击穿 [[Redis蒋德钧_26_缓存问题之雪崩,击穿,穿透#2 击穿的引发和解决]]
	- 会给数据库造成巨大压力. 我们可以解决的办法是, 不要给热点数据设置过期时间.
	- 我们最好在接入层增加流量控制, 一旦检测到有大流量请求访问, 就开启限流, 避免被大流量压垮.
