---
title: 17_Redis阻塞之绑核解决
date: 2022-09-08
categories: Redis
tags: 归档笔记/学习笔记/Redis
author: 蒋德钧
mtime: 2024-09-30
---

## 学习之前

## Redis 阻塞之绑核解决

### 1. 现代 CPU 架构图以及可能会增加处理时间的时间点

#### 服务器 CPU 架构图

- 一个 **CPU** 有多个**物理核心**(10 - 20 个), 一个 CPU 包含他自己连接的**内存**, 不同的 CPU 之间通过**总线**连接
- 一个物理核有一个私有的**一级指令/数据缓存,** 一个私有的**二级缓存**, 与其它物理核心共用同一个 CPU 内的**三级缓存**;
- 一个物理核心可以分为两个**逻辑核**, 也叫**超线程**, 每个**线程**运行在一个逻辑核上 .
![[附件/Excalidraw/Drawing 2022-08-31 16.17.31Redis_阻塞和绑核.excalidraw|1200]]

![[附件/image/17_Redis阻塞之绑核解决.jpg]]

#### NUMA 架构

如果一个程序首先在一个 SOCKET(处理器) 上运行, 并把数据保存在这个 SOCKET 对应的内存中, 然后被调度到别的 SOCKET, 此时, 就需要访问之前 SOCKET 的内存, 这就叫远端内存访问, 与访问直接连接的内存相比, 远端访问会增加延迟. 由于访问本地内存和远端内存的时间不一致, 就被称作为 Non-Uniform Memory Access(NUMA)

#### 多核 CPU 对 Redis 性能的影响

应用程序本身会把运行时信息 (栈指针, CPU 核的寄存器值等) 记录, 并把访问频繁的指令和数据缓存到 L1, L2 上 (为了快速访问)

在多核场景下转换运行 CPU 被称为上下文切换 (context switch), 这需要重新加载数据和指令到 L1, L2 缓存上,显然会耗费时间.

- 案例:
	- 99% 尾延迟 : 99% 的命令小于的执行时间, 比如 100 个命令执行时间从小到大排列, 第 100 个就是 99 % 尾延迟. 强调的是大多数命令都要小于某个值
	- get 和 put 的尾延迟各有要求, 发现是 CPU 上下文切换次数比较多, 导致延迟较大.
	- 绑核命令: 把 -c 0 表示要绑的编号

		```
		taskset -c 0 ./redis-server
		```

#### NUMA 架构对 Redis 性能的影响

一种常见的做法, 把网络中断处理程序和 CPU 核绑定, 保证中断处理程序不在 CPU 核上来回跳动, 可以提升网络处理性能. 那么 Redis 绑在哪个核上呢?

epoll 机制下的 Redis 读取指令并完成的模型

![[附件/image/17_Redis阻塞之绑核解决-1.jpg]]

如果此时网络中断和 Redis 绑的不是一个处理器 SOCKET, 那么 Redis 就要跨处理器访问内存, 会花费比较多的时间.

> 解决: 最好把网络中断程序和 Redis 实例绑在同一个 CPU Socket 上, 这样可以从本地内存读取网络数据.

### 2. 绑核的风险和解决方案

#### 如何绑核和如何不绑错核

1. 查看核心编号

```
lscpu

Architecture: x86_64
...
NUMA node0 CPU(s): 0-5,12-17
NUMA node1 CPU(s): 6-11,18-23
...

```

0 - 5 是第一个 socket 的 1 号逻辑核的编号, 12 - 17 是 2 号逻辑核的编号

- 不要想当然的认为 0 - 11 就是同一个 socket

#### 绑核的风险和解决方案

- 风险
	- 如果把 Redis 绑在一个逻辑核上, 就会造成子进程, 后台进程和 Redis 竞争资源, 导致主线程阻塞
- 解决方案
	1. 一个 Redis 实例绑定跟一个物理核: 即两个逻辑核都绑上
	2. 更改 Redis 源码: 使子线程后台线程不在同一个核上运行
	3. Redis 6.0 出来后，可以支持 CPU 核绑定的配置操作了，我将在第 38 讲中向你介绍 Redis 6.0 的最新特性。

#### 更改 Redis 源码

这个方案就是通过修改 Redis 源码，把子进程和后台线程绑到不同的 CPU 核上。

如果你对 Redis 的源码不太熟悉，也没关系，因为这是通过编程实现绑核的一个通用做法。学会了这个方案，你可以在熟悉了源码之后把它用上，也可以应用在其他需要绑核的场景中。

接下来，我先介绍一下通用的做法，然后，再具体说说可以把这个做法对应到 Redis 的哪部分源码中。

通过编程实现绑核时，要用到操作系统提供的 1 个数据结构 cpu_set_t 和 3 个函数 CPU_ZERO、CPU_SET 和 sched_setaffinity，我先来解释下它们。

- cpu_set_t 数据结构：是一个位图，每一位用来表示服务器上的一个 CPU 逻辑核。
- CPU_ZERO 函数：以 cpu_set_t 结构的位图为输入参数，把位图中所有的位设置为 0。
- CPU_SET 函数：以 CPU 逻辑核编号和 cpu_set_t 位图为参数，把位图中和输入的逻辑核编号对应的位设置为 1。
- sched_setaffinity 函数：以进程 / 线程 ID 号和 cpu_set_t 为参数，检查 cpu_set_t 中哪一位为 1，就把输入的 ID 号所代表的进程 / 线程绑在对应的逻辑核上。
那么，怎么在编程时把这三个函数结合起来实现绑核呢？很简单，我们分四步走就行。
- 第一步：创建一个 cpu_set_t 结构的位图变量；
- 第二步：使用 CPU_ZERO 函数，把 cpu_set_t 结构的位图所有的位都设置为 0；
- 第三步：根据要绑定的逻辑核编号，使用 CPU_SET 函数，把 cpu_set_t 结构的位图相应位设置为 1；
- 第四步：使用 sched_setaffinity 函数，把程序绑定在 cpu_set_t 结构位图中为 1 的逻辑核上。
下面，我就具体介绍下，分别把后台线程、子进程绑到不同的核上的做法。
先说后台线程。为了让你更好地理解编程实现绑核，你可以看下这段示例代码，它实现了为线程绑核的操作：

```c
//线程函数
void worker(int bind_cpu){
    cpu_set_t cpuset;  //创建位图变量
    CPU_ZERO(&cpu_set); //位图变量所有位设置0
    CPU_SET(bind_cpu, &cpuset); //根据输入的bind_cpu编号，把位图对应为设置为1
    sched_setaffinity(0, sizeof(cpuset), &cpuset); //把程序绑定在cpu_set_t结构位图中为1的逻辑核

    //实际线程函数工作
}

int main(){
    pthread_t pthread1
    //把创建的pthread1绑在编号为3的逻辑核上
    pthread_create(&pthread1, NULL, (void *)worker, 3);
}
```

对于 Redis 来说，它是在 bio.c 文件中的 bioProcessBackgroundJobs 函数中创建了后台线程。bioProcessBackgroundJobs 函数类似于刚刚的例子中的 worker 函数，在这个函数中实现绑核四步操作，就可以把后台线程绑到和主线程不同的核上了。

和给线程绑核类似，当我们使用 fork 创建子进程时，也可以把刚刚说的四步操作实现在 fork 后的子进程代码中，示例代码如下：

```c
int main(){
   //用fork创建一个子进程
   pid_t p = fork();
   if(p < 0){
      printf(" fork error\n");
   }
   //子进程代码部分
   else if(!p){
      cpu_set_t cpuset;  //创建位图变量
      CPU_ZERO(&cpu_set); //位图变量所有位设置0
      CPU_SET(3, &cpuset); //把位图的第3位设置为1
      sched_setaffinity(0, sizeof(cpuset), &cpuset);  //把程序绑定在3号逻辑核
      //实际子进程工作
      exit(0);
   }
   ...
}
```

对于 Redis 来说，生成 RDB 和 AOF 日志重写的子进程分别是下面两个文件的函数中实现的。

- rdb.c 文件：rdbSaveBackground 函数；
- aof.c 文件：rewriteAppendOnlyFileBackground 函数。
这两个函数中都调用了 fork 创建子进程，所以，我们可以在子进程代码部分加上绑核的四步操作。
使用源码优化方案，我们既可以实现 Redis 实例绑核，避免切换核带来的性能影响，还可以让子进程、后台线程和主线程不在同一个核上运行，避免了它们之间的 CPU 资源竞争。相比使用 taskset 绑核来说，这个方案可以进一步降低绑核的风险。

## 评论区收获和思考

问题：在一台有两个 CPU Socket（每个 Socket 8 个物理核）的服务器上，我们部署了一个有着 8 个实例的 Redis 切片集群（8 个实例都为主节点，没有主备关系），现在有两个方案：

1. 在同一个 CPU Socket 上运行 8 个实例，并和 8 个 CPU 核绑定；
2. 在两个 CPU Socket 上各运行 4 个实例，并和相应 Socket 上的核绑定。

 - 建议选择 2:
	 - 如果都绑在一个 CPU 上, 共享 L3 缓冲会导致命中率降低, 影响性能.
	 - 共用一个 socket 一定会造成内存竞争, 如果有不够的情况, 一定会产生跨 socket 的内存访问, 影响性能.
