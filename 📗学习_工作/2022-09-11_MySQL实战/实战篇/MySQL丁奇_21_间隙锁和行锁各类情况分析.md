---
tags: 归档笔记/学习笔记/MySQL
title: 21_间隙锁和行锁各类情况分析
date: 2022-09-14
categories: MySQL
author: 丁奇
mtime: 2024-09-25
---

## 前言

- 两个原则, 两个优化, 一个 bug
1. 原则 1：加锁的基本单位是 next-key lock。next-key lock 是前开后闭区间。(a,b]
2. 原则 2：查找过程中访问到的对象才会加锁。
3. 优化 1：索引上的等值查询，给唯一索引加锁的时候，next-key lock 退化为行锁。又叫记录锁
4. 优化 2：索引上的等值查询，向右遍历时且最后一个值不满足等值条件的时候，next-key lock 退化为间隙锁。
5. 一个 bug：唯一索引上的范围查询也会访问到不满足条件的第一个值为止。

- 表

```sql
CREATE TABLE `t` (
  `id` int(11) NOT NULL,
  `c` int(11) DEFAULT NULL,
  `d` int(11) DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `c` (`c`)
) ENGINE=InnoDB;

insert into t values(0,0,0),(5,5,5),
(10,10,10),(15,15,15),(20,20,20),(25,25,25);
```

id 主键, c 普通索引

## 间隙锁和行锁各类情况分析 8 案例

### 案例一: 等值查询

![[附件/image/21_间隙锁和行锁各类情况分析-1663539009700.jpeg]]

1. 没有 7, 锁住 (5, 10]
2. 根据优化 2, 不满足, 退化成 (5, 10)
3. 结论: B 被锁住, C 没有

### 案例二: 非唯一索引等值锁

![[附件/image/21_间隙锁和行锁各类情况分析-1663540142805.jpeg]]

1. 访问到 c, 所以锁住 (0, 5], (5,10], 优化得到 (5,10)
2. 因为是覆盖索引, 所以**没有访问主键**, session B 可以正常执行, 主键上没有锁; session C 被锁住; 如果改成 `select d from t where c = 5 lock in share mode`, 则会被锁住了
3. lock in share mode 只锁覆盖索引, 但是如果是 **for update** , 系统认为你接下来要更新数据, 就会给**主键索引上满足条件的行**加上行锁.
4. 这个例子的启示: 锁是加在索引上的, 如果要避开覆盖索引的优化, 需要人为对语句改造.

### 案例三: 主键索引范围锁

```sql
mysql> select * from t where id=10 for update;
mysql> select * from t where id>=10 and id<11 for update;
```

锁范围一样吗?

![[附件/image/21_间隙锁和行锁各类情况分析-1663540839767.jpeg]]

1. 开始执行的时候，要找到第一个 id=10 的行，因此本该是 next-key lock(5,10]。 根据优化 1， 主键 id 上的等值条件，退化成行锁，只加了 id=10 这一行的行锁。
2. 范围查找就往后继续找，找到 id=15 这一行停下来，因此需要加 next-key lock(10,15]。

### 案例四: 非唯一索引范围锁

![[附件/image/21_间隙锁和行锁各类情况分析-1663541106622.jpeg]]

在第一次用 c=10 定位记录的时候，索引 c 上加了 (5,10] 这个 next-key lock 后，由于索引 c 是非唯一索引，没有优化规则，也就是说不会蜕变为行锁，因此最终 sesion A 加的锁是，索引 c 上的 (5,10] 和 (10,15] 这两个 next-key lock。

### 案例五: 唯一索引范围锁 (BUG)

![[附件/image/21_间隙锁和行锁各类情况分析-1663541186013.jpeg]]

1. 先判断出 (10, 15], 其实扫描到这里, 已经够了. 但是
2. 又继续往下扫描, 到 (15, 20], 因为是范围扫描, 也不会优化, 所以总范围就是 (10, 20]

### 案例六: 非唯一索引上存在 " 等值 " 的例子

```sql
CREATE TABLE `t` (
  `id` int(11) NOT NULL,
  `c` int(11) DEFAULT NULL,
  `d` int(11) DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `c` (`c`)
) ENGINE=InnoDB;

insert into t values(0,0,0),(5,5,5),
(10,10,10),(15,15,15),(20,20,20),(25,25,25);
```

```sql
mysql> insert into t values(30,10,30);
```

所以现在索引 c 的情况是:

![[附件/image/21_间隙锁和行锁各类情况分析-1663541671712.jpeg]]

因为索引 c 上存着主键索引, 所以是不会出现完全相同的**两行**的

所以两条记录之间, 也是存在间隙的.

![[附件/image/21_间隙锁和行锁各类情况分析-1663541737485.jpeg]]

这时，session A 在遍历的时候，先访问第一个 c=10 的记录。同样地，根据原则 1，这里加的是 (c=5,id=5) 到 (c=10,id=10) 这个 next-key lock。然后，session A 向右查找，直到碰到 (c=15,id=15) 这一行，循环才结束。根据优化 2，这是一个等值查询，向右查找到了不满足条件的行，所以会退化成 (c=10,id=10) 到 (c=15,id=15) 的间隙锁。也就是说，这个 delete 语句在索引 c 上的加锁范围，就是下图中蓝色区域覆盖的部分。

![[附件/image/21_间隙锁和行锁各类情况分析-1663541828295.jpeg]]

(5, 15)

### 案例七: limit 语句

![[附件/image/21_间隙锁和行锁各类情况分析-1663541874142.jpeg]]

可以看到, B 语句通过了

这是因为，案例七里的 delete 语句明确加了 limit 2 的限制，因此在遍历到 (c=10, id=30) 这一行之后，满足条件的语句已经有两条，循环就结束了。因此，索引 c 上的加锁范围就变成了从（c=5,id=5) 到（c=10,id=30) 这个前开后闭区间，如下图所示：

![[附件/image/21_间隙锁和行锁各类情况分析-1663541923245.jpeg]]

这个例子对我们实践的指导意义就是，在删除数据的时候尽量加 limit。这样不仅可以控制删除数据的条数，让操作更安全，还可以减小加锁的范围。

### 案例八: 一个死锁

来体现 next -key lock 其实是间隙锁和行锁加起来的结果.

![[附件/image/21_间隙锁和行锁各类情况分析-1663541962184.jpeg]]

1. session A 启动事务后执行查询语句加 lock in share mode，在索引 c 上加了 next-key lock(5,10] 和间隙锁 (10,15)；
2. session B 的 update 语句也要在索引 c 上加 next-key lock(5,10] ，进入锁等待；
3. 然后 session A 要再插入 (8,8,8) 这一行，被 session B 的间隙锁锁住。由于出现了死锁，InnoDB 让 session B 回滚。

 其实是这样的，session B 的“加 next-key lock(5,10] ”操作，实际上分成了两步，先是加 (5,10) 的间隙锁，加锁成功；然后加 c=10 的行锁，这时候才被锁住的。也就是说，我们在分析加锁规则的时候可以用 next-key lock 来分析。但是要知道，具体执行的时候，是要分成间隙锁和行锁两段来执行的。

## 锁和行锁各类情况分析思考

- 还是我们在文章开头初始化的表 t，里面有 6 条记录，图 12 的语句序列中，为什么 session B 的 insert 操作，会被锁住呢？

![[附件/image/间隙锁和行锁各类情况分析.jpg]]

从右往左扫描, 25, 20, 15, 10 才停下, 因为不是唯一索引, 所以要扫描锁到 5, 故索引 c 锁范围为 (5, 25)

因为 select * , 所以主键索引上, 锁 15, 20 两个行锁 (满足条件的).
